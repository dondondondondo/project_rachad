<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?android:attr/colorControlHighlight"> <!-- Couleur du ripple qui s'adapte au thème -->

    <!-- Fond visible au repos -->
    <item>
        <shape android:shape="oval">
            <solid android:color="@android:color/transparent"/> <!-- Fond transparent -->
            <stroke
                android:width="1dp"
                android:color="?android:attr/colorForeground"/> <!-- Bordure optionnelle -->
        </shape>
    </item>

    <!-- Masque pour contraindre l'effet ripple à la forme circulaire -->
    <item android:id="@android:id/mask">
        <shape android:shape="oval">
            <solid android:color="@android:color/white"/> <!-- Couleur arbitraire (masquée) -->
        </shape>
    </item>

</ripple>